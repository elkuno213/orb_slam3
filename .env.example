# ORB-SLAM3 Docker Compose Environment Variables
#
# Copy this file to .env and adjust paths for your setup.
# All variables have sensible defaults and are optional.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your paths
#   docker compose up orb-slam3-dev

# ─── Source Code ──────────────────────────────────────────────────────────────
# Path to the ORB-SLAM3 source directory on the host.
# Used as the Docker build context and bind-mounted into the dev container.
# When running Docker-out-of-Docker (DooD) from within another container,
# set this to the host path where the source code resides.
# Default: current directory (.)
# ORB_SLAM3_SRC_DIR=/home/user/orb_slam3

# ─── Datasets ────────────────────────────────────────────────────────────────
# Path to the datasets directory on the host (mounted read-only).
# Supports EuRoC, KITTI, TUM RGB-D, and TUM-VI datasets.
# Default: ./datasets
# DATASETS_DIR=/data/slam-datasets

# ─── Results ─────────────────────────────────────────────────────────────────
# Path to the results directory on the host (mounted read-write).
# SLAM trajectory outputs and evaluation results are written here.
# Default: ./results
# RESULTS_DIR=/data/slam-results

# ─── Evaluation ──────────────────────────────────────────────────────────────
# Path to the evaluation scripts directory on the host (mounted read-write).
# Contains verify.py and baseline.json for regression testing.
# Default: ./evaluation
# EVALUATION_DIR=./evaluation

# ─── Display ─────────────────────────────────────────────────────────────────
# X11 display for Pangolin viewer (runtime and dev services only).
# Default: :0
# DISPLAY=:0

# X authority file for X11 authentication.
# Default: $HOME/.Xauthority
# XAUTHORITY=/home/user/.Xauthority
